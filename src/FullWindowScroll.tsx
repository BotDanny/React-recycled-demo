import React from 'react'

export default function FullWindowScroll() {
    const ref = React.useRef();
    // document.body.ref
    return (
        <div style={{
            height: "10000px",
            width: "100%"
        }}>
            dawad
        </div>
    )
}
